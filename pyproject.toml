[project]
name = "task-forge-lite"
version = "0.1.0"
description = "TaskForge Lite - Simple backend service for managing tasks without database"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.0.0",
    "pytest>=8.0.0",
]